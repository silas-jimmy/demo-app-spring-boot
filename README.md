# Demo web application with Springboot

This project demonstrates a simple web application using the Springboot framework.

## Getting started

1. Clone the project.

2. Navigate to the `demo` folder and run the following:

```
./mvnw spring-boot:run
```

3. Open the [localhost](http://localhost:8080) address to view the output.

